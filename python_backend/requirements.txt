# Core Framework
fastapi
uvicorn[standard]
gunicorn

# Pydantic and AI
pydantic[email]
# model.py imports pydantic_ai.models.anthropic unconditionally (used when a
# model name contains "claude"), so the anthropic extra is required at import
# time even though most default model configs use google/openrouter.
pydantic-ai-slim[openrouter,google,anthropic]
griffe

# Database and Storage
supabase

# Caching
redis

# Vector Database
pinecone

# AI Models
openai
httpx

# Document Processing
pypdf2
python-docx
python-pptx
Pillow

# Authentication & Encryption
bcrypt
python-jose[cryptography]
cryptography

# AWS S3 (optional)
boto3

# Utilities
python-multipart
python-dotenv
python-dateutil
